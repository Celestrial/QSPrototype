<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="0dp"
        android:layout_marginTop="0dp"
        android:orientation="vertical">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="64dp">

            <TextView
                android:id="@+id/textView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="@string/inspections"
                android:textAlignment="center"
                android:textSize="36sp"
                android:textStyle="bold" />
        </FrameLayout>

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <ImageButton
                    android:id="@+id/ResidentialButton"
                    android:layout_width="match_parent"
                    android:layout_height="222dp"
                    android:adjustViewBounds="false"
                    android:contentDescription="@string/residential_report_selection"
                    android:cropToPadding="true"
                    android:scaleType="centerCrop"
                    tools:src="@mipmap/ic_launcher_foreground"
                    tools:srcCompat="@mipmap/ic_launcher_foreground" />

                <ImageButton
                    android:id="@+id/IndustrialButton"
                    android:layout_width="match_parent"
                    android:layout_height="221dp"
                    android:contentDescription="@string/industrial_report_selection"
                    android:scaleType="fitXY"
                    tools:src="@mipmap/industrial_button_2" />

            </LinearLayout>
        </ScrollView>

    </LinearLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/floatingActionButton"
        android:layout_width="116dp"
        android:layout_height="116dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="21dp"
        android:clickable="true"
        android:focusableInTouchMode="true"
        app:backgroundTint="@android:color/background_light"
        app:backgroundTintMode="src_atop"
        app:rippleColor="@color/industrial_button_2bg"
        app:srcCompat="@android:drawable/sym_def_app_icon" />
</RelativeLayout>